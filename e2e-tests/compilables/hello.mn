# RUN: %sammine_exe% --file %s  | FileCheck %s --implicit-check-not . 
# Use `let` keyword
let x : f64 = 0;

extern printd(x : f64);
# this is a function
fn f(x : f64) {
     x + 2 + 3 +1;
    # x;
     x = 2;
     x = 2;
     printd(2.0);
}
